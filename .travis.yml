language: bash

env:
  global:
    - MAKEFLAGS="-j 2"

script: pwd && git clone https://github.com/buildroot/buildroot ../buildroot && make

notifications:
  email:
    on_success: change # default: change
    on_failure: change # default: always
    
#Thanks to @duckinator for this idea. Now build, damnit!
